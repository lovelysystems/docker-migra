# Tagged Migra Container

## Building

Set the [migra](https://pypi.org/project/migra/) version in [requirements.in](./requirements.in) and build the docker image:

```
./gradlew buildDockerImage
```
